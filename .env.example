# =============================================================================
# Frontend muhiti (Vite). Nusxa oling: cp .env.example .env
# To'liq API konteksti: docs/api/CONTRACT.md
# =============================================================================

# Backend API ning to'liq baz URL i (Oxirida "/" QO'YMASNING).
#
# LOCAL (tavsiya):
#   Bo'sh qoldiring — npm run dev paytida /api so'rovlari Vite proksi orqali
#   localhost:3000 ga yo'naltiriladi (vite.config.ts).
#
# LOCAL (bevosita backend):
#   VITE_API_ORIGIN=http://localhost:3000
#   Eslatma: backendda CORS uchun http://localhost:5173 ruxsat berilgan bo'lishi kerak.
#
# PRODUCTION:
#   VITE_API_ORIGIN=https://api.sizning-domen.uz

# VITE_API_ORIGIN=
